
<!DOCTYPE html>
<html>
  <head>
    <title>PCFG Support</title>
    <%= render 'layouts/head' %>
    <%= stylesheet_link_tag('/support/styles') %>
  </head>
  <body>
<!-- header section starts -->
    <%= render 'layouts/header' %>
<!-- header section ends -->
<!-- main section starts -->
   <main style="background: linear-gradient(to bottom, #000000, #1a0d0d, #1a0d0d, #000000)">
    <div class="container">
      <h1><%= @power_supply.model %></h1>
      <% if @power_supply.image.attached? %>
        <div>
          <%= image_tag(@power_supply.image) %>
        </div>
      <% end %>
      <div class="main__component-info">
        <p>Бренд: <%= @power_supply.brand %></p>
        <p>Цена: <%= number_to_currency(@power_supply.price) %></p>
      </div>
      <div class="action-buttons__all">
        <%= link_to "Edit component", edit_power_supply_path(@power_supply) %>
        <%= button_to 'Delete component', power_supply_path(@power_supply), method: :delete %><br>
        <%= link_to "All power supplies", power_supplies_path %>
      </div>
    </div>
   </main>
<!-- main section ends -->
<!-- footer section starts -->
    <%= render 'layouts/footer' %>
<!-- footer section ends -->
    </div>
  </body>
</html>
